# CASTING in Python refers to the conversion of one data type to another.
# Python provides several built-in functions for casting, including int(), float(), str(), list(), tuple(), set(), and dict().
# Examples of casting:
# 1. Converting a string to an integer
str_num = "10"